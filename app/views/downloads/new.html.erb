<% provide(:title, 'New download') %>
<h1>New download</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@download) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create download", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
